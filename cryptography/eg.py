def solution(A):
    sort_item = sorted()
    for i in range(len(sort_item)):
        sort_item[i] == i + 1
        if sort_item[i]:
            i += 1
        else:
            return sort_item[i]

result= solution([2,1,3,5])
print(result)
        
    